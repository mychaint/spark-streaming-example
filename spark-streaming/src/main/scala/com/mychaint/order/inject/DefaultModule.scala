package com.mychaint.order.inject

import com.google.inject.AbstractModule

private[inject] final class DefaultModule(devEmails: String) extends AbstractModule {

  override def configure(): Unit = {

  }
}
